<?php
// E:\PAYROLL\config\db_credentials.php.example
//
// On shared hosts (e.g. InfinityFree) that cannot set environment variables,
// copy this file to config/db_credentials.php and fill in the real values.
// The real file is gitignored so the credentials never enter the repository.
//
// Priority in config/DBconnect.php:
//   1. Environment variables  DB_HOST / DB_PORT / DB_USER / DB_PASSWORD /
//      DB_NAME / DB_SSL / DB_SSL_CA
//   2. This file (config/db_credentials.php)
//   3. Local development defaults (localhost / root / empty / wip0)

$db_host = 'sql310.infinityfree.com';
$db_port = '';
$db_user = 'if0_42499165';
$db_pass = 'your-password-here';
$db_name = 'if0_42499165_wip0';
$db_ssl = '';

// For cloud hosts that require TLS (e.g. Aiven):
//   $db_host = 'mysql-xxxx-remielbaking19-xxxx.a.aivencloud.com';
//   $db_port = '25158';
//   $db_user = 'avnadmin';
//   $db_pass = 'your-password-here';
//   $db_name = 'defaultdb';
//   $db_ssl  = '1';
